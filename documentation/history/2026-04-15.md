# 2026-04-15

## Accomplished

- Fixed MIME type hardcoded as `image/png` in OpenAI-compatible service; all image formats (JPG, WebP, GIF, BMP, AVIF, SVG) now send the correct `image/*` MIME type in the data URL.
- Fixed model dropdown showing a selectable "(not found)" option when the saved model is unavailable; replaced with a disabled placeholder and a warning message.
- Eliminated settings UI double-render on open; `loadInstalledModels` now surgically updates only the model dropdown wrapper instead of re-rendering all four settings sections.

## Key Decisions

- Added `IMAGE_MIME_TYPES` record to `constants.ts` as the single source of truth for extension→MIME mapping.
- `AiProviderService.transcribeImage` interface updated to accept `mimeType: string` as a required second parameter; Ollama ignores it (`_mimeType`) since it sends raw base64 without a MIME prefix.
- Used a stable `modelDropdownContainerEl` wrapper div to enable targeted DOM updates without touching the rest of the settings UI.
- `modelAvailable` guard set to `true` when `installedModels` is empty (still loading), preventing false "not found" state before the async fetch completes.

## Domain Model Changes

- `AiProviderService.transcribeImage` signature: `(imageData, mimeType, prompt)` — new required `mimeType` parameter.
- `IMAGE_MIME_TYPES` added to `src/app/domain/constants.ts`.

## Open Questions / Follow-ups

- Consider adding a dedicated setting to enable/disable unchanged-image skip independently of overwrite mode (carry-over from 2026-04-14).
- Evaluate normalizing legacy service file paths for cleaner module naming.
