# 2026-04-20

## Accomplished

- Refactored duplicated transcription handler functions to eliminate code duplication between `register-events.ts` and `register-nn-menus.ts`.
- Created new shared module `src/app/commands/transcription-handlers.ts` with `transcribeFileWithNotice` and `transcribeFolderWithProgress` functions.
- Updated `src/app/commands/register-events.ts` to import and use shared handlers.
- Updated `src/app/commands/register-nn-menus.ts` to import and use shared handlers.
- Removed unused imports (`ProgressNotice`, `log`) from updated files.

## Key Decisions

- Extracted common handler functions to `transcription-handlers.ts` module following the principle of DRY (Don't Repeat Yourself).
- Kept the module in the `commands/` directory for logical grouping.
- Maintained original function signatures and behavior - pure refactoring, no functional changes.

## Files Changed

- `src/app/commands/transcription-handlers.ts` — new module with shared handlers
- `src/app/commands/register-events.ts` — removed duplicate functions, added import from shared module
- `src/app/commands/register-nn-menus.ts` — removed duplicate functions, added import from shared module

## Open Questions / Follow-ups

- None. The refactoring is complete and addresses the open item from 2026-04-19.
