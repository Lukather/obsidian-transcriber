# 2026-04-19

## Accomplished

- Added Notebook Navigator menu integration to display obsidian-transcriber commands in Notebook Navigator's context menus
- Created `src/app/commands/register-nn-menus.ts` with Notebook Navigator API detection and menu registration
- Updated `src/app/plugin.ts` to register and cleanup NN menus on plugin load/unload
- Uses NN API version 1.2.0+ for file/folder menu hooks
- Icons use Obsidian conventions: `file-text` for single image transcription, `files` for folder batch transcription

## Key Decisions

- NN integration is optional: if Notebook Navigator is not installed, the plugin works normally without errors
- Menu items Only show when NN API is available (version >= 1.2.0)
- Duplicated transcription logic (transcribeFileWithNotice, transcribeFolderWithProgress) in register-nn-menus.ts to avoid circular dependencies - future refactor opportunity
- Uses type assertion for `app.plugins.plugins` access since it's not in Obsidian's type definitions

## Files Changed

- `src/appplugin.ts` — added NN menu registration and cleanup
- `src/app/commands/register-nn-menus.ts` — new module for NN integration

## Open Questions / Follow-ups

- Consider refactoring to share transcription handler functions between register-events.ts and register-nn-menus.ts
